---
phase: 01-foundation-interview-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - src/app/page.tsx
  - src/components/SetupForm.tsx
  - src/components/ResumeBanner.tsx
  - src/components/AppHeader.tsx
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - INT-06

must_haves:
  truths:
    - "Interviewer can enter name, age, background and click Begin Interview to create a session"
    - "If localStorage has an existing session, ResumeBanner appears with Resume / Start new options"
    - "Resume from file opens a file picker, loads valid JSON, and navigates to interview screen"
    - "Invalid JSON file shows inline error message"
    - "Auto-save indicator shows Saved timestamp after session creation"
  artifacts:
    - path: "src/components/SetupForm.tsx"
      provides: "Setup form with name, age, background fields and Begin Interview CTA"
      exports: ["SetupForm"]
    - path: "src/components/ResumeBanner.tsx"
      provides: "Conditional banner for existing localStorage session"
      exports: ["ResumeBanner"]
    - path: "src/components/AppHeader.tsx"
      provides: "Header with app name, person name, AutoSaveIndicator, Export button"
      exports: ["AppHeader"]
    - path: "src/app/page.tsx"
      provides: "Root page: shows SetupForm or redirects to /interview based on session state"
      contains: "useSessionStore"
  key_links:
    - from: "src/components/SetupForm.tsx"
      to: "src/lib/store.ts"
      via: "useSessionStore().createSession()"
      pattern: "createSession"
    - from: "src/components/ResumeBanner.tsx"
      to: "src/lib/store.ts"
      via: "useSessionStore().session"
      pattern: "useSessionStore"
    - from: "src/app/page.tsx"
      to: "src/app/interview/page.tsx"
      via: "router.push('/interview') after session created"
      pattern: "router.push.*interview"
---

<objective>
Build the session setup screen: the centered setup form, the resume-from-localStorage banner, and the resume-from-file flow. This is the entry point every interviewer sees.

Purpose: Establishes the session before the interview screen can render. Implements D-01 through D-03, D-14 through D-18 from CONTEXT.md.
Output: Fully functional setup screen with session creation, localStorage resume detection, and JSON file import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-interview-flow/01-CONTEXT.md
@.planning/phases/01-foundation-interview-flow/01-UI-SPEC.md
@.planning/phases/01-foundation-interview-flow/01-01-SUMMARY.md

<interfaces>
From src/lib/types.ts:
```typescript
export type DomainKey = "medical" | "financial" | "daily" | "relationships" | "end-of-life";
export type DomainStatus = "not-started" | "in-progress" | "complete" | "skipped";
export interface SessionData {
  id: string;
  createdAt: string;
  person: { name: string; age: number; backgroundNotes: string; };
  transcript: Partial<Record<DomainKey, Turn[]>>;
  domainStatus: Record<DomainKey, DomainStatus>;
  currentDomain: DomainKey;
  currentQuestionIndex: number;
  archiveGeneratedAt?: string;
  archive?: Archive;
}
```

From src/lib/store.ts:
```typescript
export const useSessionStore: {
  session: SessionData | null;
  lastSavedAt: string | null;
  createSession: (person: SessionData['person']) => SessionData;
  loadSession: (data: SessionData) => void;
  clearSession: () => void;
}
```

From src/lib/session.ts:
```typescript
export function exportSessionToFile(session: SessionData): void;
export async function loadSessionFromFile(file: File): Promise<SessionData | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SetupForm component with validation</name>
  <read_first>
    src/lib/types.ts,
    src/lib/store.ts,
    src/components/ui/button.tsx,
    src/components/ui/card.tsx,
    src/components/ui/textarea.tsx,
    .planning/phases/01-foundation-interview-flow/01-UI-SPEC.md (Setup Screen section)
  </read_first>
  <files>src/components/SetupForm.tsx</files>
  <behavior>
    - Renders 3 fields: "Person's name" (text input), "Age" (number input), "Brief background" (textarea, 4 rows)
    - "Begin Interview" button is disabled until name and age are non-empty
    - On submit: calls useSessionStore().createSession({ name, age: Number(age), backgroundNotes }) then router.push('/interview')
    - "Resume from file" is a text link below the button; clicking opens a hidden file input (accept=".json")
    - On file selected: calls loadSessionFromFile(file); if null shows inline error "This file doesn't look like a valid session. Please choose a different file."; if valid calls loadSession(data) then router.push('/interview')
    - Loading state: "Begin Interview" button shows spinner + "Preparing interview..." text while disabled
    - Background textarea placeholder: "A few sentences about their life, health situation, and family context. This helps personalize the questions."
  </behavior>
  <action>
Create `src/components/SetupForm.tsx` as a `"use client"` component.

Layout per UI-SPEC.md Setup Screen:
- Outer: `min-h-screen flex flex-col bg-stone-50`
- Content area: `flex-1 flex items-center justify-center px-6 py-16`
- Card: `w-full max-w-lg` using shadcn Card with `p-8` (2xl = 48px per spacing scale)

Form fields:
- Name: `<label>Person's name</label>` + `<input type="text" className="..." />` (use shadcn Input if available, else styled input)
- Age: `<label>Age</label>` + `<input type="number" min="1" max="120" />`
- Background: `<label>Brief background</label>` + shadcn `<Textarea rows={4} placeholder="..." />`

Buttons:
- "Begin Interview": shadcn Button default variant, sky-500 bg (`bg-sky-500 hover:bg-sky-600 text-white`), full width
- "Resume from file": plain `<button>` styled as text link (`text-stone-500 text-sm underline`)
- Hidden `<input type="file" accept=".json" ref={fileInputRef} onChange={handleFileChange} className="hidden" />`

Validation:
- `disabled={!name.trim() || !age}` on Begin Interview button
- File error state: `const [fileError, setFileError] = useState<string | null>(null)` — render below file link as `<p role="alert" className="text-red-600 text-sm mt-1">{fileError}</p>`

Loading state:
- `const [loading, setLoading] = useState(false)` — set true on submit, false after router.push
- Button content: `{loading ? <><Loader2 className="w-4 h-4 animate-spin mr-2" />Preparing interview...</> : 'Begin Interview'}`

Typography per UI-SPEC:
- Page title above card: `<h1 className="text-[28px] font-semibold text-stone-900 mb-8 text-center">Advance Directive</h1>`
- Labels: `text-sm font-medium text-stone-700`
- Input/textarea: `text-base text-stone-900`
  </action>
  <verify>
    <automated>cd "e:/claude-code/advance-directive" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `"use client"`
    - File contains `createSession` call
    - File contains `router.push('/interview')`
    - File contains `loadSessionFromFile`
    - File contains `role="alert"` for file error
    - File contains `aria-label` or label elements for all 3 fields
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SetupForm renders 3 fields, validates before enabling CTA, creates session on submit, handles file resume with error state.</done>
</task>

<task type="auto">
  <name>Task 2: ResumeBanner, AppHeader, and root page wiring</name>
  <read_first>
    src/components/SetupForm.tsx,
    src/lib/store.ts,
    src/lib/session.ts,
    .planning/phases/01-foundation-interview-flow/01-UI-SPEC.md (Header section, Interaction Design — ResumeBanner)
  </read_first>
  <files>
    src/components/ResumeBanner.tsx,
    src/components/AppHeader.tsx,
    src/app/page.tsx,
    src/app/interview/page.tsx
  </files>
  <action>
Create `src/components/ResumeBanner.tsx` as `"use client"`:
- Props: `{ onResume: () => void; onStartNew: () => void }`
- Renders full-width banner: `role="banner" aria-label="Previous session detected"`
- Background: `bg-sky-50 border-b border-sky-200`
- Content: heading "You have a session in progress" + body "Would you like to continue where you left off?"
- Two buttons: "Resume session" (sky-500 Button default) + "Start new session" (ghost Button)
- Dismiss: clicking either button calls the respective handler

Create `src/components/AppHeader.tsx` as `"use client"`:
- Props: `{ personName?: string }`
- Height 56px: `h-14 bg-white border-b border-stone-200 flex items-center px-6`
- Left: "Advance Directive" in `text-sm font-semibold text-stone-700`
- Center: `{personName && <span className="text-sm text-stone-500">{personName}</span>}`
- Right: AutoSaveIndicator (inline) + Export button (outline variant)
- AutoSaveIndicator: reads `useSessionStore().lastSavedAt`, renders `aria-live="polite"` span: "Saved · {HH:MM}" in `text-sm text-green-600`; on save, briefly sets opacity-50 then back to opacity-100 via `useEffect` watching `lastSavedAt`
- Export button: calls `exportSessionToFile(session)` if session exists; shows toast "Nothing to export yet. Complete at least one answer first." if no session (use a simple `alert()` for MVP — no toast library needed)

Create `src/app/page.tsx` as `"use client"`:
- Reads `useSessionStore().session`
- If session exists AND we're on setup page: show ResumeBanner above SetupForm
- ResumeBanner onResume: `router.push('/interview')`
- ResumeBanner onStartNew: `clearSession()` then dismiss banner (hide it)
- Render: `<AppHeader />` + `{showBanner && <ResumeBanner ... />}` + `<SetupForm />`

Create `src/app/interview/page.tsx` as a placeholder (Plan 03 will fill it):
```tsx
"use client"
export default function InterviewPage() {
  return <div className="p-8 text-stone-500">Interview screen — coming in Plan 03</div>
}
```

Accessibility per UI-SPEC:
- ResumeBanner: `role="banner"` + `aria-label="Previous session detected"`
- AutoSaveIndicator: `aria-live="polite"` + `aria-label="Auto-save status"`
  </action>
  <verify>
    <automated>cd "e:/claude-code/advance-directive" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ResumeBanner.tsx` contains `role="banner"` and `aria-label="Previous session detected"`
    - `src/components/AppHeader.tsx` contains `aria-live="polite"`
    - `src/app/page.tsx` contains `useSessionStore` and `ResumeBanner`
    - `src/app/interview/page.tsx` exists
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Setup screen fully wired: AppHeader with auto-save indicator, ResumeBanner for existing sessions, SetupForm with file resume. Root page navigates to /interview after session creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→file input | User-supplied JSON file parsed client-side |
| client→localStorage | Session read on mount to detect existing session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | loadSessionFromFile | mitigate | Validate id + person + transcript fields before accepting; return null on invalid |
| T-02-02 | Information Disclosure | AppHeader export | accept | User exports their own data; no server involved |
| T-02-03 | Denial of Service | malformed JSON file | mitigate | try/catch in loadSessionFromFile returns null; error shown to user |
</threat_model>

<verification>
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `src/components/SetupForm.tsx` contains `createSession` and `loadSessionFromFile`
- `src/components/ResumeBanner.tsx` contains `role="banner"`
- `src/components/AppHeader.tsx` contains `aria-live="polite"`
- `src/app/page.tsx` renders both ResumeBanner and SetupForm
- No file contains `NEXT_PUBLIC_ANTHROPIC`
</verification>

<success_criteria>
Setup screen renders at `/`. Interviewer can fill name/age/background and click Begin Interview to create a session and navigate to /interview. If localStorage has a prior session, ResumeBanner appears. Resume from file loads valid JSON or shows error. AppHeader shows auto-save timestamp.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-interview-flow/01-02-SUMMARY.md`
</output>
